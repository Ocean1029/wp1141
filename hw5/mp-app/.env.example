# ---------------------------------------------------------------------------
# APP ENVIRONMENT VARIABLES
# This file can be directly used for local development (.env.dev)
# For production, create .env.prod and update values accordingly
# ---------------------------------------------------------------------------

# Application Environment
NODE_ENV=development

# ---------------------------------------------------------------------------
# DATABASE (Development Defaults)
# db-dev is the service name in docker-compose.dev.yml
# ---------------------------------------------------------------------------

POSTGRES_USER=Y
POSTGRES_PASSWORD=Y_dev_password
POSTGRES_DB=Y

# Local dev Postgres URL for Prisma / Next.js
DATABASE_URL="postgresql://Y:Y_dev_password@db-dev:5432/Y"

# ---------------------------------------------------------------------------
# NEXTAUTH CONFIGURATION
# NEXTAUTH_URL for dev always points to local frontend
# NEXTAUTH_SECRET can be any random string for dev (not required to be strong)
# ---------------------------------------------------------------------------

NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="dev-secret-key-replace-if-needed"

# ---------------------------------------------------------------------------
# OAUTH PROVIDERS (Optional for Dev)
# Leave empty for dev if you don't need OAuth locally.
# App will still run without these values.
# ---------------------------------------------------------------------------

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
FACEBOOK_CLIENT_ID=
FACEBOOK_CLIENT_SECRET=

# ---------------------------------------------------------------------------
# PUSHER (Optional for Dev)
# If not using realtime features in dev, these can remain empty.
# NOTE: NEXT_PUBLIC_* variables are exposed to the browser.
# ---------------------------------------------------------------------------

PUSHER_APP_ID=
PUSHER_KEY=
PUSHER_SECRET=
PUSHER_CLUSTER=ap3

NEXT_PUBLIC_PUSHER_KEY=
NEXT_PUBLIC_PUSHER_CLUSTER=ap3
